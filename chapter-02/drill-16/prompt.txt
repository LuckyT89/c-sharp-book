Give an example of a good way to use var and an example of a bad way to use var. Don't need to do actual example.

In the example the following line is added at the top of Program.cs so that you can use some types in this namespace:
using System.Xml;